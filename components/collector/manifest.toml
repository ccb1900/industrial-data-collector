# 组件包自描述：本包提供的组件类型元数据（分类/能力/显示名）。
# 经 go:embed 在包 init 时注册进 internal/pluginmeta——中心类型表由
# 各包聚合生成，不手工维护。
name = "collector"
title = "Collector"

[[types]]
name = "csv-collector"
kind = "collector"
capability = "collector"
title = "CSV Collector"

